#version 450

layout (local_size_x = 256) in;

struct ComputeData
{
    int Value;
};

layout(std430, binding = 0) buffer layout_ComputeData
{
    ComputeData Data[];
};

void main() 
{
    uint index = gl_GlobalInvocationID.x;
	Data[index].Value = Data[index].Value * 2;
}
